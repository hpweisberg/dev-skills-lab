<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Web Dev Skills</title>
</head>
<body>
  <h1>Web Dev Skills:</h1>
  <h3>Current time: <%= time %></h3>
  <ul>
    <% skills.forEach(skill => { %>
      <li>
        <form 
          action="/skills/<%= skill._id %>?_method=DELETE"
          class="delete-form"
          method="POST"
          >
        <button type="submit">X</button>
        </form>
        <a href="/skills/<%= skill._id %>"><%= skill.language %></a>
        -
        <%= skill.knowledge ? 'has knowledge' : 'does not have knowledge' %> 
      </li>
      <% }) %>
  </ul>
  <a href="./">Home</a>
  <a href="/skills/new">Add To-Do</a>
</body>
</html>